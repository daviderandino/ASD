#include <stdio.h>
#include "complex.h"

int main() {
    Complex a,b,c;
    // ......
    c = prod(a,b);

    // QUASI ADT
    // non c'è una totale invisibilità: il client può ad esempio fare c = prod(a,b)

}
